import React from 'react';

export function WinPage() {
  // const { isLoose } = useTypedSelector((state) => state.gameStatus);
  return (
    <div>This is win page</div>
    //   <div
    //     className={
    //       isLoose ? style.loosePageWrapper : style.loosePageWrapperHidden
    //     }
    //   ></div>
  );
}
